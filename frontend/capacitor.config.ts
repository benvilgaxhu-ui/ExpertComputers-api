import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.expertcomputers.app',
  appName: 'Expert Computers',
  webDir: 'build'
};

export default config;
